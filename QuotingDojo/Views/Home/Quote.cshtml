@{
    ViewData["Title"] = "Home Page";
}
<!DOCTYPE html>
<html>
    <head>
        <meta charset='utf-8'>
        <title>Quotes</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/1.11.8/semantic.min.css"/>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/1.11.8/semantic.min.js"></script>
        <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.2.0.min.js"></script>
        <style>
            #wrapper {
                padding: 10%;
            }
            button {
                display: inline;
            }
            img {
                width: 20%;
            }
        </style>
    </head>
    <body>
        <div id="wrapper" class="ui container">
            <div class="column">
                <div class="ui inverted segment black">
                    <h2>Here are the awsome quotes!</h2>
                    <!--Looping through the quotes in the database-->
                    @{ foreach (var item in @ViewBag.quotes) {
                        
                        <ul>
                            <li>@item["quote"]
                                <ul>
                                    <li>@item["name"] at @item["created_at"]</li>
                                </ul>            
                            </li>
                        </ul>
                        }
                    }
                </div>
            </div>
        </div>
    </body> 
</html>